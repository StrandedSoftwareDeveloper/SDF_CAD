An SDF mesher written in zig 0.15.2 using the algorithm described in [Araújo & Jorge "Adaptive polygonization of implicit surfaces"](https://www.researchgate.net/publication/220252153_Adaptive_polygonization_of_implicit_surfaces).

Run with `zig build run > out.stl`
